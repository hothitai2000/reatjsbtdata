import React from "react";
export default function Water(props){
    return(
        <div className="container d-flex justify-content-around align-Waters-center">;
        <h4>{props.name}</h4>
        <p>{props.price}</p>

        </div>
    )
}